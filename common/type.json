{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Check if Annotation type feature implemented",
  "description": "'An Annotation must have 1 or more types' (Section 3.1)",
  "assertionType": "must", 
  "expectedResult": "valid",
  "errorMessage": "Error: Annotation does not have a 'type' (string or array).",
  "type": "object",
  "properties": {
    "type": { "type": ["string", "array"] }
  },
  "required": ["type"]  
}
