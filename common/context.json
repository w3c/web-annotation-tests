{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Check if @context feature implemented",
    "description": "'The Annotation must have 1 or more @context values' (Section 3.1)",
    "assertionType": "must",
    "expectedResult": "valid",
    "errorMessage": "Error: Annotation does not have an @context (string/uri or array).",
    "type": "object",
    "properties": {
      "@context": { "type": ["string", "array"] }
    },
    "required": ["@context"]
}
