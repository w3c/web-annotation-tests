{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "SpecificResource Definition",
    "description": "An oa:SpecificResource is recognized by having at least one of the following keys described in Section 4 of the model: selector, state, purpose (without value), scope, styleClass, renderedVia. This schema does not check values of SpecificResource properties",
    "type": "object",
    "properties": {
        "selector": {},
        "state": {},
        "styleClass": {},
        "renderedVia": {},
        "scope": {},
        "purpose": {}
    },
    "anyOf": [
        {"required": ["selector"]},
        {"required": ["state"]},
        {"required": ["styleClass"]},
        {"required": ["renderedVia"]},
        {"required": ["scope"]},
        {
            "required": ["purpose"],
            "not": {"required": ["value"]}
        }
    ]
}
