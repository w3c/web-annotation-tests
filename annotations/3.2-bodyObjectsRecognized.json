{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "3.2-bodyObjectsRecognized.json",
    "assertionType": "must",
    "expectedResult": "valid",
    "onUnexpectedResult" : "failAndContinue",
    "errorMessage": "ERROR: One or more Bodies of the Annotation is not one of a string of format uri, an External Web Resource, a Choice or Set, a Specific Resource.",    
    "title": "Each Body is one of a string of format uri, an External Web Resource, a Choice or Set, a Specific Resource.",
    "description": "True when all Body(ies) are one of a string of format uri, an External Web Resource, a Choice or Set, a Specific Resource (Sections 3.2, Section 4)",
    "type": "object",
    "properties": 
      {
        "body":
           {"oneOf": [
              { "$ref": "bodyTarget.json#/definitions/bodyResourcesFound" },
              { "type" : ["array"], 
                "items": { "$ref": "bodyTarget.json#/definitions/bodyResourcesFound" } 
             }
           ]      
        }
    }            
}