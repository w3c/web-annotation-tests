{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "3.2.1-bodyETBLanguage.json",
    "assertionType": "may",
    "expectedResult": "valid",
    "onUnexpectedResult" : "passAndContinue",
    "errorMessage": "The description of an Embedded Textual Body may include language key with one or more values that are each a language code.",    
    "title": "Implements **Embedded Textual Body _language_ key** with one or more values each of which is a **language code** [model 3.2.1](https://www.w3.org/TR/annotation-model/#bodies-and-targets)",
    "description": "True when Annotation includes one or more Embedded Textual Bodies, one or more of which include language key with one or more values that are each a language code. (Section 3.2.1)",
    "type": "object",
    "properties":
    {
        "body": 
        {
           "oneOf": [
                      { "allOf": 
                        [
                         { "$ref": "bodyTarget.json#/definitions/textualBodyFound" },
                         { "$ref": "bodyTarget.json#/definitions/languageValidIfPresent"},
                         {  "required": ["language"] }
                         ]
                       },
                       { "not":
                         {
                           "items": 
                            { "not":
                               { "allOf": 
                                [
                                  { "$ref": "bodyTarget.json#/definitions/textualBodyFound" },
                                  { "$ref": "bodyTarget.json#/definitions/languageValidIfPresent"},
                                  {  "required": ["language"] }
                                 ]
                               }
                             }     
                         }                          
                       }
                     ]                       
         }
     }
}